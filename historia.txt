git rm --cached archivos.txt
Este comando nos sirve para quitar un add de memoria ram

git show archivos.txt
Muestra los cambios que ha tenido el repo

git reset id_commit --hard
Este comando se devuelve a commit especificado